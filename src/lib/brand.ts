/**
 * Non-localized brand constants (name, assets).
 * UI-facing brand copy lives in messages/{locale}.json under the `brand` namespace.
 */
export const BRAND = {
  name: "steelTech",
  /** Official favicon — https://steeltechsyria.com/favicon.ico */
  faviconPath: "/favicon.ico",
} as const;
